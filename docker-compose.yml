version: '3.5'

services:
    movefiles:
        build:
            args:
                CONTAINER_NAME: movefiles 
                CONTAINER_VERSION: 1.0.5
                FROM_BASE: perl-carton:latest
            cache_from:
                - perl-carton:latest
                - perl-5.24.3:latest
                - base_container:latest
                - alpine:3.6                 
            context: .
            dockerfile: Dockerfile
        environment:
            DEBUG_TRACE: ${DEBUG_TRACE:-0}
        hostname: movefiles
        image: movefiles:latest
        logging:
            driver: json-file
            options:
                max-file: "3" 
                max-size: "10m"
#        restart: unless-stopped
        volumes:
            - ./:/usr/src/myapp
